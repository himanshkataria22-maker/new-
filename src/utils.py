"""
Some utility functions.
"""

def greet(name):
    """Say hello to someone."""
    return f"Hello, {name}!"

def format_version(version):
    """Add v prefix to version."""
    return f"v{version}"
